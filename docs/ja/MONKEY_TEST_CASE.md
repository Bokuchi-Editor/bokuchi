# モンキーテストケースリスト

自動テストではカバーしきれないUI統合動作を手動で確認するためのテストケース。
本来はE2Eで実現したいが、今はTauriの対応状況的に不可能なので手動で行う。

## 1. タブ操作

| #    | テストケース                    | 操作手順                                       | 期待結果                                                |
| ---- | ------------------------------- | ---------------------------------------------- | ------------------------------------------------------- |
| M-01 | 新規タブ作成                    | `Ctrl+N` を押す                                | 新しいタブが開き、エディタにフォーカスが当たる          |
| M-02 | 複数タブ切り替え                | 3つ以上タブを開き、各タブをクリック            | 正しい内容が表示され、エディタにフォーカスが移る        |
| M-03 | Ctrl+Tab タブ切り替え           | `Ctrl+Tab` で次タブ、`Ctrl+Shift+Tab` で前タブ | 順序通りに切り替わり、末尾→先頭のラップも動作する       |
| M-04 | タブの閉じ（未変更）            | 未変更タブの×ボタンをクリック                  | 確認なしで即座に閉じる                                  |
| M-05 | タブの閉じ（変更あり）          | 内容を編集後、×ボタンをクリック                | 「保存しますか？」ダイアログが表示される                |
| M-06 | 保存ダイアログ - 保存           | M-05のダイアログで「保存」を選択               | ファイルが保存されてタブが閉じる                        |
| M-07 | 保存ダイアログ - 保存しない     | M-05のダイアログで「保存しない」を選択         | 保存せずタブが閉じる                                    |
| M-08 | 保存ダイアログ - キャンセル     | M-05のダイアログで「キャンセル」を選択         | ダイアログが閉じ、タブは残る                            |
| M-09 | 同一ファイルの重複オープン防止  | 同じファイルを2回開く                          | 2つ目は既存タブにフォーカスが移る（新タブは作られない） |
| M-10 | タブのドラッグ&ドロップ並べ替え | タブをドラッグして順序を変更                   | タブの並び順が変わり、永続化される                      |
| M-11 | 最後のタブを閉じたときの空状態  | 残り1つのタブを閉じる                          | アプリアイコン・最近のファイル・アクションボタンを含む空状態画面が表示される |
| M-12 | 空状態から新規ファイル作成      | 空状態で「新規ファイル」ボタンをクリック        | 新しいタブが開き、エディタにフォーカスが当たる          |
| M-13 | 空状態からファイルを開く        | 空状態で「ファイルを開く」ボタンをクリック      | ファイルダイアログが開き、選択したファイルが新しいタブで開かれる |
| M-14 | 空状態から最近のファイルを開く  | 空状態で最近のファイルリストの項目をクリック    | 選択したファイルが新しいタブで開かれる                  |

## 2. ファイル操作

| #    | テストケース                     | 操作手順                                       | 期待結果                                                                                          |
| ---- | -------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| M-15 | ファイルを開く                   | `Ctrl+O` → ファイル選択                        | ファイル内容がタブに表示され、成功通知が出る                                                      |
| M-16 | ファイル保存                     | `Ctrl+S`                                       | 現在のタブが保存され、タイトルの変更マーク(●)が消える                                             |
| M-17 | 名前を付けて保存                 | `Ctrl+Shift+S`                                 | ダイアログで保存先を選択後、新しいファイル名でタブが更新                                          |
| M-18 | マークダウンファイルのドロップ   | `.md`ファイルをエディタ領域にドラッグ&ドロップ | ファイルが新しいタブで開かれる                                                                    |
| M-19 | 非マークダウンファイルのドロップ | `.txt`ファイルをドロップ                       | 適切に処理される（エラーにならない）                                                              |
| M-20 | OSファイル関連付けで開く         | OSのファイルエクスプローラで`.md`ファイルをダブルクリック | アプリ未起動時は起動してファイルを開く。起動済みの場合は既存ウィンドウの新しいタブで開かれる |

## 3. オートセーブ

| #    | テストケース           | 操作手順                                            | 期待結果                                   |
| ---- | ---------------------- | --------------------------------------------------- | ------------------------------------------ |
| M-21 | オートセーブ発動       | 設定でオートセーブON → 既存ファイルを編集 → 3秒待つ | 自動保存され、成功通知が出る               |
| M-22 | オートセーブ無効       | 設定でオートセーブOFF → 編集 → 3秒待つ              | 自動保存されない                           |
| M-23 | 新規タブでオートセーブ | 新規タブ（未保存ファイル）で編集 → 3秒待つ          | オートセーブは発動しない（パスがないため） |

## 4. 設定

| #    | テストケース               | 操作手順                                                          | 期待結果                                                                               |
| ---- | -------------------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| M-24 | 設定画面を開く             | `Ctrl+,` を押す                                                   | 設定ダイアログが開く                                                                   |
| M-25 | テーマ変更                 | 設定でテーマを変更                                                | 即座にUIに反映される                                                                   |
| M-26 | 言語変更                   | 設定で言語を変更                                                  | UI全体のテキストが切り替わる                                                           |
| M-27 | タブレイアウト変更         | 水平↔垂直を切り替え                                               | タブバーの表示方向が変わる                                                             |
| M-28 | 設定の永続化               | テーマを変更 → アプリ再起動                                       | 変更したテーマが保持されている                                                         |
| M-29 | テーマギャラリーとホバープレビュー | 設定 > 外観を開き、プリセットテーマカードに約0.2秒ホバー          | カードに名前・light/darkバッジ・7色のパレットストリップが表示され、アプリのミニプレビュー付きポップオーバーが出る（エディタ部分はテーマ色ではなくMonaco固定のlight/dark配色のまま） |
| M-30 | 複製によるカスタムテーマ作成 | プリセットカードにホバーし複製アイコンをクリック（または「+ 新規テーマ」カードからベースを選択） | カスタムテーマ「<名前> のコピー」（UI言語により表記は変わる）がカスタムテーマ欄に作成され、即座に適用され、インプレースのテーマエディタが開く |
| M-31 | 適用中カスタムテーマのライブ編集 | 新しいカスタムテーマを適用した状態で、カラーチップ/HEX欄で色を変更し、エディタモードのLight/Darkを切り替え | 色の変更がすべて即座にアプリ全体に反映される（設定画面自体も即座に再配色される）。モード切替でMonacoエディタ部分がlight（vs）↔dark（vs-dark）で切り替わる |
| M-32 | カスタムテーマの永続化     | カスタムテーマの名前を変更し、色をいくつか編集 → アプリ再起動     | カスタムテーマが編集後の色と名前のまま適用されている。ステータスバーのテーマメニューにもプリセットの下に表示される |
| M-33 | コントラスト警告とベースの色に戻す | テーマエディタでテキストと背景をほぼ同じ色に設定 → 「ベースの色に戻す」をクリック | カードとエディタに非ブロッキングの ⚠ 警告が表示される（極端な色も設定自体は可能）。リセットでベースプリセットの7色に戻り、警告が消える |
| M-34 | 適用中カスタムテーマの削除 | テーマエディタで削除をクリックし、確認ダイアログで確定            | ダイアログに適用中のテーマである旨の警告が表示される。削除後はDefaultテーマにフォールバックする（カスタムテーマ適用中に「Defaultテーマに戻す」ボタンが機能することも確認） |
| M-35 | フォントサイズ変更         | エディタのフォントサイズを変更                                    | エディタのフォントが即座に反映                                                         |
| M-36 | Mermaidレンダリング有効化  | 設定 > 詳細 > レンダリング拡張でMermaidをONに切り替え             | 設定が保存され、mermaidフェンスドコードブロックがプレビューで図表として描画される       |
| M-37 | KaTeXレンダリング無効化    | 設定 > 詳細 > レンダリング拡張でKaTeXをOFFに切り替え              | 数式（$...$, $$...$$）がプレビューでraw テキストとして表示される                        |
| M-38 | レンダリング設定の永続化   | レンダリングトグルを変更 → アプリ再起動                           | 変更したレンダリング設定が保持されている                                               |
| M-39 | ミニマップ切り替え         | 設定 > エディタでミニマップをトグル                                | エディタ右側のミニマップが即座に表示/非表示になる                                      |
| M-40 | 全表示モードでのミニマップ | ミニマップを有効にし、エディタ・分割・プレビューモードを切り替え  | エディタを含むすべてのモードでミニマップが表示される                                   |
| M-41 | テーブル変換設定           | テーブル変換設定（自動/確認/オフ）を変更 → アプリ再起動           | 再起動後も設定が保持されている                                                         |

## 5. 検索

| #    | テストケース                         | 操作手順                                                                 | 期待結果                                                                   |
| ---- | ------------------------------------ | ------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| M-42 | 検索パネルを開く                     | `Ctrl+F` を押す                                                          | 検索パネルが開き、検索入力にフォーカスが当たる                             |
| M-43 | 検索＆置換パネルを開く               | `Ctrl+H` を押す                                                          | 検索パネルが置換入力付きで開く                                             |
| M-44 | 全タブ検索ショートカット             | `Ctrl+Shift+F` を押す                                                    | 「全タブ検索」がデフォルトでチェックされた状態で検索パネルが開く           |
| M-45 | 全タブ検索チェックボックスの表示     | 2つ以上のタブを開き `Ctrl+F` を押す                                      | 「全タブ検索」チェックボックスが表示される                                 |
| M-46 | 全タブ検索チェックボックスの非表示   | タブが1つだけの状態で `Ctrl+F` を押す                                    | 「全タブ検索」チェックボックスが表示されない                               |
| M-47 | クロスタブ検索結果                   | 3つ以上のタブに共通テキストを入力し、「全タブ検索」を有効にして検索      | 結果がタブごとにグループ化され、マッチ数が表示される                       |
| M-48 | クロスタブ検索結果のナビゲーション   | クロスタブ結果で `Enter` を押すか、別タブのマッチをクリック              | 対応するタブがアクティブになり、カーソルがマッチ位置にジャンプする         |
| M-49 | 検索オプション（大小文字/単語/正規表現） | 大文字小文字・単語単位・正規表現をトグルして検索                     | 単一タブ・クロスタブの両方で選択したオプションが検索結果に反映される       |
| M-50 | 現在のタブで置換                     | 置換を開き、置換テキストを入力、置換/すべて置換をクリック                | 現在のタブのみでテキストが置換される                                       |

## 6. 表示モード

| #    | テストケース       | 操作手順                          | 期待結果                                          |
| ---- | ------------------ | --------------------------------- | ------------------------------------------------- |
| M-51 | 表示モード切り替え | `Ctrl+Shift+D` を繰り返し押す     | split → preview → editor → split とローテーション |
| M-52 | 同期スクロール     | splitモードでエディタをスクロール | プレビューが連動してスクロールする                |
| M-177 | 表示モードが再起動後も維持される | Editor（または Preview）表示に切り替え、アプリを完全終了して再起動 | 終了時と同じ表示モードで起動する（常に split で起動しない）。初回起動（保存値なし）は従来どおり split が既定 |

## 7. キーボードショートカット

| #    | テストケース         | 操作手順              | 期待結果                       |
| ---- | -------------------- | --------------------- | ------------------------------ |
| M-53 | ヘルプ表示           | `F1` を押す           | ヘルプダイアログが開く         |
| M-54 | 最近のファイル       | `Ctrl+R` を押す       | 最近のファイルダイアログが開く |
| M-55 | アウトラインパネル   | `Ctrl+Shift+O` を押す | アウトラインパネルが開閉する   |
| M-56 | フォルダツリーパネル | `Ctrl+Shift+E` を押す | フォルダツリーパネルが開閉する |

## 8. アウトライン & フォルダツリー

| #    | テストケース                     | 操作手順                             | 期待結果                             |
| ---- | -------------------------------- | ------------------------------------ | ------------------------------------ |
| M-57 | アウトライン見出しジャンプ       | アウトラインパネルで見出しをクリック | エディタが該当行にスクロール         |
| M-166 | プレビュー単独表示でもアウトラインジャンプが効く *(#376)* | 見出しが複数あるドキュメントをPreview（プレビュー単独）表示にする → アウトラインパネルで見出しをクリック | プレビューが該当見出しまでスクロールする。以前はプレビュー単独表示では何も起きなかった（ジャンプは非表示のエディタのみを駆動していた）。Editor/Split表示は従来どおりジャンプする |
| M-58 | アウトライン自動更新             | エディタで見出しを追加/削除          | アウトラインパネルがリアルタイム更新 |
| M-59 | フォルダツリーからファイルを開く | フォルダツリーでファイルをクリック   | タブにファイルが開かれる             |
| M-60 | フォルダツリー展開/折りたたみ    | フォルダのアイコンをクリック         | サブツリーが展開/折りたたまれる      |

## 9. 外部ファイル変更検知

| #    | テストケース          | 操作手順                             | 期待結果                       |
| ---- | --------------------- | ------------------------------------ | ------------------------------ |
| M-61 | 外部変更検知          | 開いているファイルを別エディタで変更 | 変更検知ダイアログが表示される |
| M-62 | 外部変更 - リロード   | M-55のダイアログで「リロード」       | 最新の内容に更新される         |
| M-63 | 外部変更 - キャンセル | M-55のダイアログで「キャンセル」     | 現在の編集内容が保持される     |
| M-64 | プレビューモードでの外部変更 | プレビューモードに切り替え → ファイルを外部で変更 → 「リロード」選択 → エディタまたはスプリットモードに切り替え | エディタに外部変更後の内容が表示される（古い内容ではない） |
| M-65 | プレビューリロード後のデータ消失防止 | M-58の後、オートセーブが発動するまで待つ | オートセーブが外部変更後の内容を保存する（古い内容ではない） |

## 10. ズーム

| #    | テストケース   | 操作手順        | 期待結果           |
| ---- | -------------- | --------------- | ------------------ |
| M-66 | ズームイン     | `Ctrl+=` を押す | UI全体が拡大される |
| M-67 | ズームアウト   | `Ctrl+-` を押す | UI全体が縮小される |
| M-68 | ズームリセット | `Ctrl+0` を押す | 100%に戻る         |

## 11. プレビューレンダリング

| #    | テストケース                       | 操作手順                                                                            | 期待結果                                                                  |
| ---- | ---------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| M-69 | Mermaid図表レンダリング            | Mermaidを有効にし、` ```mermaid ` フェンスドブロック（例: `graph TD; A-->B`）を記述  | プレビューにSVGとして図表が描画される                                     |
| M-70 | Mermaidエラーハンドリング          | 無効なmermaidブロック（例: `invalid!!!`）を記述                                      | 図表の代わりに赤いエラーメッセージが表示される                            |
| M-71 | Mermaidダークモードテーマ          | Mermaid図表を表示中にダークモードを切り替え                                         | 図表がダークテーマで再描画される                                          |
| M-72 | KaTeX数式レンダリング              | KaTeX有効の状態で `$E=mc^2$` と `$$\sum_{i=1}^{n} i$$` を記述                      | インライン・ディスプレイ数式が正しくレンダリングされる                    |
| M-73 | 無効化時のraw表示                  | KaTeXとMermaidを両方無効にし、数式とmermaidブロックを記述                            | `$...$` テキストと ` ```mermaid ` コードブロックがそのまま表示される      |
| M-74 | Mermaid付きHTMLエクスポート        | Mermaidを有効にし図表を記述、HTMLエクスポート                                       | エクスポートされたHTMLにMermaid図表がインラインSVGとして含まれる          |
| M-75 | HTMLエクスポート時の画像サイズ     | 大きな画像を含むMarkdownをHTMLエクスポートし、ブラウザで開く                        | 画像がコンテナ幅に収まり、アスペクト比が維持される                       |
| M-76 | KaTeX付きHTMLエクスポート *(リグレッション)* | KaTeXを有効にし複数の数式（例: `$$\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}$$`、`pmatrix`、`\nabla \cdot \mathbf{E}`）を記述、HTMLエクスポートして**オフライン**のブラウザで開く | 各数式がKaTeXフォントで一度だけ正しく描画される。横に崩れた文字の重複が出ず、インターネット接続なしでも数式が表示される |
| M-77 | HTMLサニタイズと正当なマークアップの両立 *(リグレッション)* | 1つの文書に生の `<img src=x onerror="alert(1)">` と `<script>alert(2)</script>` を書き、さらに Mermaid 図・KaTeX 数式・相対パスのローカル画像（`![](images/x.png)`）・タスクリスト（`- [x] done`）を記述。プレビューを表示し、続けて HTML エクスポートして開く | プレビュー・エクスポート後のファイルのどちらでも `alert` ダイアログが出ない。Mermaid 図（ノードのラベル含む）・数式・ローカル画像・チェックボックスはすべて正しく描画される |
| M-169 | HTMLエクスポートのコードブロック背景が均一 *(リグレッション)* | ダークテーマで、シンタックスハイライト対象の言語のフェンスドコードブロック（例: ` ```js `）を記述し、HTMLエクスポートしてブラウザで開く | 内側のコード領域と外枠の `<pre>` がまったく同じ背景色になる。内側だけが明るい四角形にならず、アプリ内プレビューと同じ見た目になる |
| M-172 | 外部(https)画像の描画 *(リグレッション — 必ずビルド版で検証)* | **ビルド版**（`tauri dev` ではなく）で、実在する公開画像を `![alt](https://raw.githubusercontent.com/.../some.png)` で参照しプレビューを表示する | リモート画像が読み込まれて表示される。**必ずビルド版で検証すること**: `tauri dev` は tauri.conf の CSP を適用しないため dev では常に表示されてしまう。`img-src` を制御する CSP はビルド版でのみ強制される |
| M-173 | CJK に隣接した太字/斜体 *(#400)* | 強調デリミタが CJK 約物/文字に接する日本語文を書く。例: `これは**「重要」**な話。`、`単なる「テキスト」から、**「アシスタント」**へと進化します。`、`「あ」*強調*へ`。あわせて `` `**コード**` ``（インラインコード）と `**コード**` を含むフェンスドコードブロックも書く。プレビュー表示し、HTMLエクスポートもする | `**…**` / `*…*` がプレビュー・エクスポート HTML の**両方**で太字/斜体になる（`*` が文字として残らない）。インラインコード・フェンスドコード内のアスタリスクはそのまま（太字化しない）。不可視文字が混入しない |

## 12. クリップボード & ペースト

| #    | テストケース                         | 操作手順                                                                  | 期待結果                                                             |
| ---- | ------------------------------------ | ------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| M-78 | HTMLテーブル貼り付け（自動モード）   | テーブル変換を「自動」に設定、スプレッドシートからテーブルをコピーして貼り付け | テーブルがMarkdownテーブル構文に自動変換される                        |
| M-79 | HTMLテーブル貼り付け（確認モード）   | テーブル変換を「確認」に設定、スプレッドシートからテーブルをコピーして貼り付け | 変換前に確認ダイアログが表示される                                   |
| M-80 | HTMLテーブル貼り付け（オフモード）   | テーブル変換を「オフ」に設定、スプレッドシートからテーブルをコピーして貼り付け | テーブルが変換されずプレーンテキストとして貼り付けられる             |
| M-81 | プレーンテキストとして貼り付け       | 書式付きテキストをコピーし、`Ctrl+Shift+V` で貼り付け                     | テーブル変換設定に関わらずプレーンテキストとして貼り付けられる       |
| M-82 | TSV/CSVデータ貼り付け                | タブ区切りまたはカンマ区切りデータをコピーし、エディタに貼り付け          | データが検出されMarkdownテーブルに変換される                         |

## 13. エッジケース・安定性

| #    | テストケース       | 操作手順                                   | 期待結果                                         |
| ---- | ------------------ | ------------------------------------------ | ------------------------------------------------ |
| M-83 | 高速タブ切り替え   | タブを素早く連打で切り替える               | クラッシュせず正しく切り替わる                   |
| M-84 | 大量タブ           | 20個以上タブを開く                         | スクロール/オーバーフロー処理が正常              |
| M-85 | 大きなファイル     | 1MB以上のMarkdownファイルを開く            | エディタが正常に動作する                         |
| M-86 | 空ファイル         | 0バイトのファイルを開く                    | エラーなく空のエディタが表示される               |
| M-87 | 長い行             | 1行が数千文字あるMarkdownファイル          | ワードラップが正しく機能する                     |
| M-88 | 長時間タブ切り替え | 画像を含むタブを50回以上切り替える         | メモリリークなし。エディタの応答性が維持される   |

## 14. Marpスライド

Marpスライドレンダリング機能（設定 > 詳細 > Marp有効化）に関するテスト。検証対象のドキュメントには `---\nmarp: true\n---` のようなフロントマターが必要。

| #    | テストケース                                                  | 操作手順                                                                                                                                            | 期待結果                                                                                                                                  |
| ---- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| M-89 | Marp設定の有効化                                              | 設定 > 詳細 > レンダリング拡張でMarpをONに切り替え                                                                                                  | 設定が保存され、`marp: true` フロントマターを持つドキュメントが通常Markdownではなくスライドとして描画される                                |
| M-90 | 連続表示モード（split）の描画                                  | Marp有効状態でMarpドキュメントをsplit表示で開く                                                                                                     | プレビュー領域に全スライドが縦に並べて描画される（スタック表示）                                                                           |
| M-91 | スライドモード（preview-only）の描画                           | Marpドキュメントをpreview-onlyに切り替える                                                                                                          | 現在のスライドのみが表示され、prev/next/グリッド/フルスクリーンの操作UIが利用可能                                                          |
| M-92 | スライドナビゲーションボタン                                   | スライドモードでprev/nextボタンをクリック                                                                                                            | スライドカウンタが更新され、表示スライドが切り替わる。範囲外（0未満や末尾超え）には進まない                                                 |
| M-93 | キーボードでのスライド送り                                     | スライドモードまたはフルスクリーンで `←` / `→` を押す                                                                                                | フォーカス位置に関係なく矢印キーでスライド間を移動できる                                                                                   |
| M-94 | サムネイル（グリッド）モード                                   | スライドモードでグリッドアイコンをクリック                                                                                                          | 全スライドがサムネイルグリッドとして表示される                                                                                             |
| M-95 | サムネイルからスライドへジャンプ                               | サムネイルモードでスライドをクリック                                                                                                                 | サムネイルモードが解除され、クリックしたスライドがカレントになる                                                                           |
| M-96 | フルスクリーンが現在のスライドを保持 *(0.8.1 リグレッション)*  | スライド N（N > 1）に移動した状態でフルスクリーンボタンをクリック                                                                                    | フルスクリーン表示が常にスライド1ではなく、現在のスライドN を表示する                                                                       |
| M-97 | 起動直後の連続モードのスクロールバー *(0.8.1 リグレッション)*   | スライド数が多いMarpドキュメントをアクティブタブにした状態でアプリを完全終了し、再起動して直後にsplitプレビューを観察                                  | ウィンドウリサイズや操作なしにMarpプレビューに縦スクロールバーが表示される                                                                 |
| M-98 | Marpのダークモード描画                                         | Marpドキュメント表示中にダークモードを切り替え                                                                                                       | スライドの背景色・文字色がダークテーマで再描画される                                                                                       |
| M-99 | 相対パス画像の描画                                             | 同一ディレクトリの画像を `![](./image.png)` で参照するMarpドキュメントを開く                                                                          | 画像がスライド内に正しく描画される（base64 インラインではなく Tauri asset protocol `asset://` でディスクから読み込まれる）                 |
| M-100 | カスタムMarpテーマフォルダの設定 | 設定 > レンダリング で、Marpテーマフォルダに `.css` ファイル（`/* @theme name */` ヘッダーありとなしを混在）を含むディレクトリを指定 | 検出された各 `@theme` 名がチップとして一覧表示され、`@theme` ヘッダーの無いファイルは無視対象として表示される。フォルダをクリアすると一覧が空になる |
| M-101 | フロントマターでカスタムテーマを適用 | テーマフォルダを設定した状態で、フロントマターで検出済みテーマを指定（`theme: <name>`）したMarpドキュメントを開く | スライドが外部CSSテーマで描画される。未知またはクリア済みのテーマ名を指定した場合はクラッシュせず組み込みの既定テーマにフォールバックする |
| M-165 | 本番ビルドでスライドが正しくスケーリングされる *(0.9.2 リグレッション)* | **本番ビルド**（`tauri:dev` ではなく `npm run tauri:build`）でMarpドキュメントを開き、split / slide / fullscreen / thumbnail の各モードで表示する | 各スライドがプレビュー幅にフィットして縮小描画される。ネイティブサイズのまま（巨大な見出し・箇条書きが右側で見切れる形で）ペインから溢れてはならない。dev では再現せずパッケージ版でのみ再現するため、ビルド済みアプリで確認すること |
| M-170 | Marpプレビューが真っ白にならない *(0.9.x リグレッション)* | 任意のMarpドキュメントをsplit表示で開く | スライドが描画される（真っ白／黒のパネルにならない）。バンドラのCJS interop（例: Vite メジャー更新）下での marp-core コンストラクタ解決を保証するケース。回帰時は DevTools に `TypeError: ... is not a constructor` が出て、無言の黒画面ではなくペインに「Failed to render Marp slides」のエラー文言が表示される |
| M-171 | 画像が多いデッキの描画とメモリ | 大きい画像を複数（合計数十MB、例 6MB の写真）参照するMarpデッキをsplit表示で開く | 全画像が `asset://` でディスクからストリーム表示される（iframe srcDoc が数MBのbase64を抱えないこと）。アプリのメモリ使用は旧base64方式より明確に少ない。なお表示画像の画素数に応じたデコード後ビットマップのメモリは増える＝これは想定どおり |
| M-178 | gaiaテーマのフォントがオフラインで表示される | **ネットワークを切断した状態**（Wi-Fi オフ）で `theme: gaia` のMarpドキュメントをプレビュー表示し、さらにHTMLとPDFにエクスポートする | 見出し・本文が Lato（ジオメトリックなサンセリフ。`a` の上部が平ら）、コードブロックが Roboto Mono で描画される。システムフォールバック（macOS では Avenir Next / セリフ付き Courier 系 mono）になってはならない。エクスポートしたHTMLをブラウザで開いた場合とPDFも同じフォントで表示される。gaia の `fonts.bunny.net` CSS `@import` を同梱 data-URI フォントで置換した対応のリグレッションケース |
| M-194 | フェンス内の `---` がエディタ⇔プレビューのスライド同期を壊さない *(リグレッション・実機確認)* | コードフェンス（``` … ```）内に `---` 行を含むMarpデッキ（例: Marp記法を解説するスライド）を開き、分割表示でスクロール | フェンス内の `---` はスライド区切りとして扱われない。スライド数がMarpの描画と一致し、フェンス以降もエディタ⇔プレビューのスクロール同期がズレない |
| M-195 | Marp PDF エクスポートで内容量の多い（テーブル）スライドがクリップされない *(リグレッション・実機確認)* | テーブル（や背の高い内容）でスライドがほぼ埋まっているスライドと、疎な中央寄せのタイトルスライドを含む Marp デッキを PDF エクスポート | テーブルのスライドは全行が欠けずに印刷される（ハードエッジで行が途切れない）。一方、疎なタイトルスライドは従来どおり縦中央寄せのまま |

## 15. プレビュー内のリンク処理

外部URLをOSブラウザにルーティングするリンククリック処理のテスト。すべて 0.8.1 のリグレッション項目（以前はリンクがiframe内で開かれたり、開けなかったりしていた）。

| #    | テストケース                                                                | 操作手順                                                                                                                  | 期待結果                                                                                                       |
| ---- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| M-102 | 通常プレビュー内の外部リンク *(0.8.1 リグレッション)*                       | 非Marpドキュメントで `[link](https://example.com)` を記述し、プレビューでクリック                                       | OSの既定ブラウザで example.com が開く。エディタ・プレビューの状態は変化しない（アプリ内ナビゲーション無し）       |
| M-103 | Marpスライド内の外部リンク *(0.8.1 リグレッション)*                         | Marpスライドに `[link](https://example.com)` を記述し、スライド/連続/フルスクリーン/サムネイルの各モードでクリック          | リンクがOSブラウザで開く。スライドのiframe自体はナビゲートしない                                                  |
| M-104 | Marp↔非Marpタブ切り替え後のリンク処理 *(0.8.1 リグレッション)*               | Marpタブを開いた後、リンクを含む通常Markdownのタブに切り替えてリンクをクリック                                              | リンクがOSブラウザで開く（条件付き再描画後でもクリックハンドラが正しく再アタッチされている）                       |
| M-105 | 内部アンカーリンク                                                          | ドキュメント内アンカー `[goto](#section)` をクリック                                                                       | 同じプレビュー内で対応する見出しまでスクロールする。ブラウザは開かない                                            |

## 16. アップデート通知

自動アップデート関連のテスト。0.8.1 では自動チェックを24時間に1回に制限し、通知ダイアログの重複表示も防止している。

| #    | テストケース                                                  | 操作手順                                                                                                                                                | 期待結果                                                                                                                                |
| ---- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| M-106 | 24時間経過後の自動チェック *(0.8.1 リグレッション)*           | 永続化された `lastUpdateCheckAt` をクリア（または初期化済みのアプリ状態で）して起動                                                                       | 初期化完了後に自動的にアップデートチェックが行われる                                                                                    |
| M-107 | 24時間以内の起動はチェックをスキップ *(0.8.1 リグレッション)* | 一度起動して自動チェックが走った状態でアプリを終了し、24時間以内に再起動                                                                                 | 2回目の起動ではサーバーへのチェックが実施されない（保存された `lastUpdateCheckAt` が利用される）                                          |
| M-108 | 最新版での手動チェック                                        | アップデートが無い状態でメニューから手動チェックを実行                                                                                                    | 「最新です」相当の通知が表示される。インストールダイアログは出ない                                                                       |
| M-109 | 通知ダイアログの重複抑制 *(0.8.1 リグレッション)*             | アップデート通知ダイアログが開いている状態で、別のチェック契機（手動・周期チェックなど）を発生させる                                                       | 2つ目のダイアログが出ず、最初のダイアログが唯一の通知として残る                                                                          |

## 17. 変数置換

グローバル変数の管理と `${var}` 置換に関するテスト。

| #     | テストケース                          | 操作手順                                                                                              | 期待結果                                                                                                |
| ----- | ------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| M-110 | グローバル変数の追加                  | 変数設定を開き、名前と値を入力して追加                                                                | 変数リストに新しい変数が表示される                                                                      |
| M-111 | グローバル変数の削除                  | 変数設定で既存の変数を削除                                                                            | リストから当該変数が消える                                                                              |
| M-112 | プレビューでの変数置換                | `name=World` を定義し、ドキュメントに `Hello ${name}` を記述                                          | プレビューに `Hello World` と表示される                                                                 |
| M-113 | 変数を展開して保存                    | 置換が含まれる状態で File > 変数を展開して保存 を実行                                                  | ディスク上のファイルが置換後の値で保存される（`${var}` プレースホルダーは残らない）                      |
| M-114 | 変数のYAMLエクスポート/インポート     | 変数のエクスポートでYAMLを書き出し、別アプリ状態でインポートしてYAMLを読み込む                          | エクスポートしたファイルに全変数が含まれており、インポートで同じ名前と値で復元される                    |

## 18. 設定のインポート / エクスポート / リセット

| #     | テストケース             | 操作手順                                                                                  | 期待結果                                                                              |
| ----- | ------------------------ | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| M-115 | 設定をJSONにエクスポート | 設定 > 詳細 > 設定エクスポートで保存先を選択                                              | 現在の設定すべてを含むJSONファイルが指定先に出力される                                |
| M-116 | 設定をJSONからインポート | 設定 > 詳細 > 設定インポートで以前エクスポートしたJSONを選択                              | 全設定が復元され、UIがインポート内容を反映して即座に更新される                        |
| M-117 | 設定のリセット           | 設定 > 詳細 > 設定リセット → 確認ダイアログで承認                                         | 全設定が初期値に戻る。テーマ・言語などの見た目が初期値に切り替わる                    |

## 19. Markdownツールバー

| #     | テストケース                          | 操作手順                                                                                                       | 期待結果                                                                                                       |
| ----- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| M-118 | ツールバーが正しい構文を挿入          | エディタでテキストを選択し、太字/斜体/見出し/リスト/リンク/画像/コードブロック/引用 等のボタンをクリック       | 選択範囲が対応するMarkdown構文で囲まれる。未選択時はプレースホルダーテキストが挿入される                       |

## 20. その他の表示設定

| #     | テストケース                              | 操作手順                                                                                                          | 期待結果                                                                                                       |
| ----- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| M-119 | タブ閉じるボタン位置                      | 設定 > タブレイアウト > 閉じるボタンの位置 を Left / Right で切り替え                                              | 各タブの×ボタンが設定に従って即座に左右へ移動する                                                              |
| M-120 | 新規タブボタンの位置                      | タブレイアウトを縦にし、設定 > タブレイアウト > 新規タブボタンの位置 を 上 / 下 で切り替える                        | 「上」はサイドバー上部ヘッダーに＋ボタンを固定。「下」は最後のタブの直下に配置し、タブが多くてスクロールする際は下端に固定（sticky）されて常に見える。再起動なしで即座に反映される。この項目は縦レイアウト時のみ表示される |
| M-121 | アウトライン表示モード                    | 設定 > インターフェース > アウトライン表示モード を 常時表示 / オーバーレイ / オフ で切り替え、アウトラインを開く     | 常時表示はエディタ隣に固定領域、オーバーレイはエディタ上に重ねて表示、オフはアウトラインを非表示にしヘッダーのボタンが薄くなる |
| M-174 | アウトラインのオフが再起動後も維持        | アウトライン表示モードを オフ にしてアプリを再起動                                                                 | 起動時にアウトラインが表示されず、設定も オフ のまま（従来の Ctrl+Shift+O の一時非表示は再起動で復活していたが、今回から永続化） |
| M-175 | ヘッダーボタンでオフから復帰              | アウトラインが オフ のとき、ヘッダーのアウトラインボタンをクリック（または Ctrl+Shift+O）                          | 直前に選んでいたスタイル（常時表示／オーバーレイ）でオンに戻り、設定も オフ から元のスタイルに切り替わる            |
| M-176 | ボタンでオフへ（永続化）                  | 常時表示でアウトライン表示中に、ヘッダーのアウトラインボタンをクリック                                             | パネルが隠れ設定が「オフ」に変わり、再起動後も維持される。オーバーレイではボタンはドロワーの開閉のみで、オフは設定からのみ設定する |
| M-122 | フォルダツリー表示モード                  | 設定 > インターフェース > フォルダツリー表示モード を Persistent / Overlay / Off で切り替え                        | Offでフォルダツリー機能が完全に非表示になる。Persistentは固定領域、Overlayはエディタ上に重ねて表示              |
| M-123 | フォルダツリーのファイルフィルタ          | フォルダを開いた状態で 設定 > インターフェース > フォルダツリーファイルフィルタ を Markdown / All に切り替え       | Markdown選択時は `.md` / `.txt` のみ表示。All選択時はフォルダ内すべてのファイルが表示される                    |
| M-124 | ワードラップ切り替え                      | 長い行を含むドキュメントを開き、設定 > エディタ > ワードラップ をトグル                                            | ON時は長い行が折り返されて表示され、OFF時は水平スクロールが必要になる                                          |
| M-125 | スクロール同期モード                      | 設定 > インターフェース > スクロール同期モード を Editor→Preview / Bidirectional / Off に切り替え、各モードでスクロール | Editor→Preview: エディタ→プレビュー方向のみ同期。Bidirectional: 双方向で同期。Off: 同期しない                |
| M-126 | スクロール同期モードの変更が再起動なしで反映される *(リグレッション)* | アプリ起動中に 設定 > インターフェース > スクロール同期モード を Bidirectional に変更（設定変更または設定リセット経由）し、**再起動せずに**エディタとプレビューの両方をスクロール | 両方向が即座に同期する（エディタをスクロールするとプレビューが追従し、プレビューをスクロールするとエディタが追従する）。アプリの再起動は不要。（エディタのスクロールリスナーがマウント時に1回だけ登録される不具合を検出） |
| M-127 | 書式バーの表示切り替え | 設定 > エディタ > 書式バー をON/OFFで切り替え（初期値ON） | エディタ上部の Markdown 書式ツールバーが、エディタを含む全てのビューモードで即座に表示/非表示される。設定は再起動後も保持される |

## 21. フォルダツリー操作

| #     | テストケース                          | 操作手順                                                                                          | 期待結果                                                                                              |
| ----- | ------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| M-128 | メニューからフォルダを開く            | フォルダ未選択状態でフォルダツリーアイコン（または File > Open Folder）をクリック                 | フォルダ選択ダイアログが開き、選択するとツリーが表示される。次回起動時にも保持されている              |
| M-129 | フォルダツリーのリフレッシュ          | 開いているフォルダ内のファイルを外部で追加・削除し、パネルのリフレッシュボタンをクリック           | フォルダツリーが外部の変更を反映して更新される                                                        |
| M-130 | コンテキストメニューでファイルをリネーム | フォルダツリーのファイルを右クリック → リネーム → 新しい名前を入力して確定                          | ディスク上でファイルがリネームされる。当該ファイルを開いていたタブが正しく追従する（パスが壊れない）  |

## 22. 再起動 & リロードの安定性

| #     | テストケース                                                                          | 操作手順                                                                                                                                                                                  | 期待結果                                                                                                       |
| ----- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| M-131 | 既定パス外のファイルがタブ復元される *(0.8.1 リグレッション — Windowsで重要)*         | `$HOME/Desktop/Documents/Downloads` 以外の場所（例: `C:\projects\foo.md`）にある `.md` ファイルをOSの関連付けで開き、保存済みタブとして表示された状態でアプリを再起動                       | タブが元のファイルパス付きで正しく復元される。新規/未保存タブに格下げされず、内容も正しい                       |
| M-132 | 外部変更リロードでmodifiedフラグが解除される *(0.8.1 リグレッション)*                  | 編集してdirty状態のファイルを外部でも変更し、変更検知ダイアログで「リロード」を選択                                                                                                          | エディタにディスク上の最新内容が反映され、変更マーク（●）が消える。直後の保存は実質no-op                       |

## 23. チェックリスト（タスクリスト） — 再発が多い領域

この領域は過去に最低 3 回デグレを起こしています。プレビュー描画 → クリックハンドラ → React state → Monaco モデル同期、と独立した複数のレイヤを経由するため、各レイヤの単体テストが通っても End-to-End では壊れているケースが見抜けません。**リリース前に下記すべて** を実施してください。

| #     | テストケース                                                                          | 操作手順                                                                                                                                                                | 期待結果                                                                                                                                                                                       |
| ----- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-133 | プレビューでチェックボックスをクリックするとエディタにも反映される *(再発リグレッション)*       | 分割表示で `- [ ] task` と入力し、プレビュー側に表示されたチェックボックスをクリック                                                                                       | プレビューがチェック済みになり、**エディタのソースも `[ ]` から `[x]` に即座に更新** される。エディタのみ表示に切り替えても変化が反映されており、保存後にディスク上の内容にも反映される         |
| M-134 | プレビュークリック後の打鍵でトグル状態が失われない *(再発リグレッション)*                  | プレビューでチェックボックスをクリックしてトグルした直後、エディタの任意の行末で文字を1つ入力                                                                              | トグル状態（`[x]`）が保持される。打鍵によって `[ ]` に静かに戻ることはない（Monaco モデルが React state と同期していることを保証）                                                              |
| M-135 | 不正なタスク行が混在してもクリック対象がズレない                                         | 分割表示で `- [ ]NoSpace`（`]` の直後にスペースなし）と `- [ ] Real`（スペースあり）の2行を書き、プレビューに表示される唯一のチェックボックスをクリック                  | 正しい書式の `- [ ] Real` が `- [x] Real` になる。不正な `- [ ]NoSpace` は**変更されない**（GFM 仕様で正しい行のみがチェックボックス描画され、クリックはその行を対象にする必要がある）         |
| M-136 | 保存・再オープン後もトグル状態が保持される                                               | プレビューでチェックボックスをトグルし、`Ctrl+S` で保存 → タブを閉じる → 再度ファイルを開く                                                                                | 再オープン後もトグル後の状態（`[x]` か `[ ]`）が表示される（トグルが Monaco の表示だけでなくディスクまで届いていることを保証）                                                                  |
| M-137 | ツールバーのチェックリストボタンが正しい GFM 構文を挿入する                              | 空行にカーソルを置きツールバーのチェックリストボタンをクリック → 続けてプレビューに表示されたチェックボックスをクリック                                                    | 行頭に `- [ ] `（末尾スペースあり）が挿入される。プレビューでチェックボックスとして描画され、クリックすると M-133 と同様にエディタソースもトグルされる                                            |
| M-138 | インデント / ネストされたチェックボックスが独立してトグルできる                          | 親タスクとインデントされた子タスクを書き（例: `- [ ] Parent` の次行に `  - [ ] Child`）、子タスクのチェックボックスをプレビューでクリック                                  | 子タスクの行のみが `[x]` に切り替わり、親は `[ ]` のまま。インデントは正確に保持される                                                                                                          |

## 24. プレビューでのテーブルセル編集

| #     | テストケース                                                                          | 操作手順                                                                                                                                                                | 期待結果                                                                                                                                                                                       |
| ----- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-139 | ダブルクリックでインラインセルエディタを開く | 分割表示またはプレビュー表示で、描画されたMarkdownテーブルの任意のセルをダブルクリック | そのセルの上に編集ボックスが開いてフォーカスされ、現在のテキストが全選択される。ボックスにはセルの生（エスケープ前）のソーステキストが表示される |
| M-140 | セルを編集して確定する | セルをダブルクリックしてテキストを変更し、`Enter` を押す | 変更がそのセルのソースにのみ書き戻され（他のセル・列は変更されない）、エディタは下のセルへ移動する |
| M-141 | スクロールが飛ばずにセル間を移動する | インラインエディタを開いた状態で `Tab` / `Shift+Tab` / `Enter` を押して複数のセルを移動 | 毎回、右 / 左 / 下の隣接セルへフォーカスが移動する。移動のたびにプレビューが飛んだりスクロールしたりしない |
| M-142 | Esc で編集をキャンセルする | セルをダブルクリックして何か入力し、`Esc` を押す | エディタが閉じ、そのセルのソーステキストは変更されないまま残る |
| M-143 | 長いセルのテキストが折り返され、エディタが自動で伸びる | 長い文章（セル幅より長い）を含むセルをダブルクリック | 編集ボックスがセル幅の中でテキストをワードラップし（描画されたテーブルと同様）、それに合わせて下方向に伸びる。入力中もカーソルは見える範囲に留まる。一定の高さを超えると伸びるのを止め、1行で横に流れる代わりにボックス内部でスクロールする |
| M-144 | スプレッドシート形式のテーブル編集モーダル | プレビューでテーブルにホバーし、右上の編集ボタンをクリック → セルの編集 / 行・列の追加・削除・並べ替えを行い、適用をクリック | モーダルの変更がMarkdownテーブルのソースに反映される。キャンセルすると破棄される |

## 25. 縦タブの固定/ホバー & 臨（りん）モード

| #     | テストケース                                   | 操作手順                                                                                                                   | 期待結果                                                                                                                                   |
| ----- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| M-145 | 縦タブのピン留め既定と解除                     | タブレイアウトを「垂直」にし、サイドバーヘッダの ＋ の左にあるピンを確認 → ピンをクリックして解除                            | 既定ではピンがON（固定）でサイドバーが常時表示。解除するとサイドバーが収納され、左端に細いマーカー（nub）が残る                            |
| M-146 | ホバーでのタブ表示と再固定                     | ピン解除（ホバーモード）の状態で、ウィンドウ左端にマウスを近づける → タブを選択 → オーバーレイ内のピンをクリック            | 左端ホバーでタブのオーバーレイがスライドイン。タブ選択またはマウスを離すと収納される。オーバーレイ内のピンを押すと再び固定表示に戻る       |
| M-147 | ピン状態の永続化                               | ピンを解除（ホバー）にして アプリ再起動                                                                                    | 再起動後もホバーモードが保持されている（固定に戻らない）                                                                                  |
| M-148 | 横タブは非表示対象外                           | タブレイアウトを「水平」に切り替える                                                                                       | 横タブにはピンUIが無く、タブバーは常時表示のまま（固定/ホバーの概念が無い）                                                                |
| M-149 | 臨モードの開始と解除によるビュー復帰           | Split表示でAppBarの3トグル（Split/Editor/Preview）左の臨ボタンをクリック → `Esc` または右上の解除ボタンで終了              | 臨に入るとタブ・ツールバー・プレビュー・アウトライン・ステータスが全て隠れEdit固定になる。解除すると元のSplit表示に戻る                    |
| M-167 | 臨モードのEscは前面のオーバーレイを先に閉じる *(#375)* | 臨モード中にダイアログ（例: 最近使ったファイル / 設定）または検索パネル（`Ctrl/Cmd+F`）を開く → `Esc` を押す                | Escはダイアログ/検索パネルのみを閉じ、臨モードは維持される。何も開いていない状態でもう一度 `Esc` を押すと臨モードを抜ける。オーバーレイと臨モードが同時に閉じることはなくなった |
| M-150 | 臨の解除ボタンの出現/漸消 *(要実機確認)*       | 臨モード中、キーボードで入力（タイピング）し続ける → 入力を止めてマウスを動かす                                            | タイピング中は右上の解除ボタンが約3秒かけてフェードアウトし、マウスを動かすと即座に再表示される                                          |
| M-151 | 臨モードは永続化されない                       | 臨モードに入ったまま アプリ再起動                                                                                          | 再起動後は通常モードで起動する（臨は復元されない）                                                                                        |
| M-152 | 臨のラベルが言語で切り替わる                   | 日本語UIと他言語UIで臨ボタンのツールチップ/ラベルを確認                                                                    | 日本語では「臨」、それ以外の言語では「Rin」と表示される                                                                                  |
| M-153 | 臨モードのエディタ幅切替                       | 臨モード中、解除ボタンの下にある幅切替ボタンを押す → フル幅にした状態でエディタ右端を確認                                  | 「1000px固定（中央寄せ）」と「フル幅」が切り替わる。フル幅でも右端のボタン列の左までで止まり、テキストがボタンに被らない                  |
| M-154 | フォルダツリー統合時もホバー挙動が一致         | フォルダツリーをON（垂直タブと統合）→ ピンを解除 → 左端にマウスを近づける                                                  | ピン/ホバーの挙動はフォルダツリーの有無で変わらない。ホバーでタブ＋フォルダツリーを含むサイドバーがオーバーレイ表示される                |
| M-168 | 臨ボタンのツールチップが左に出て下のボタンに被らない | 臨モード中、右上の解除ボタン、続いてその下の幅切替ボタンにマウスホバーする                                                | 各ツールチップ（「臨モードを終了」/ 幅切替ラベル）がボタンの**左側**に表示され、下のボタンに被らない。下のボタンがクリック可能なまま保たれる |
| M-199 | サイドバー幅を範囲内でドラッグ調整 *(要実機確認)* | 垂直タブを固定（ピンON）した状態で、サイドバー右端を左右にドラッグ（フォルダツリー統合時も同様に）                        | 約180px〜約480pxの間で自由にリサイズでき、両端で止まる。タブとフォルダツリーが幅に追従し、エディタ/プレビューもリアルタイムに再レイアウトされる |
| M-200 | 最小幅を超えて狭めるとサイドバーが隠れる（Arc風） *(要実機確認)* | 右端を最小幅よりさらに大きく左へドラッグする                                                                              | 最小幅まではクランプされ、そこから明確にさらに狭めると（約120px未満）ピンが外れてホバー/自動非表示モード（左端のnub）になる。ホバーオーバーレイのピンで再固定すると直前の有効幅で復帰する（極端に細いままにはならない） |
| M-201 | サイドバー幅の永続化と適用範囲 *(要実機確認)*   | 幅を調整してアプリを再起動。さらにピンを外して左端にホバー                                                                | 再起動後も調整した幅で表示される。ホバーオーバーレイと統合サイドバー（タブ＋フォルダツリー）も同じ幅で表示される                          |

## 26. エディタ内のMarkdownテーブル編集

Monacoエディタ内でのキーボード操作によるMarkdownテーブル編集（セクション24のプレビュー側セル編集とは別物）。カーソルがGFMテーブルブロック内にある必要があり、IME変換中はオーバーライドが抑制される。

| #     | テストケース                               | 手順                                                                                                           | 期待結果                                                                                                                                          |
| ----- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-155 | Tab / Shift+Tab でのセル移動               | テーブルのセルにカーソルを置き `Tab` を数回 → `Shift+Tab` で戻る                                                | `Tab` で次セルの内容が選択され、行末では区切り行を飛ばして次行の先頭セルへ折り返す。`Shift+Tab` は逆方向に移動し、先頭ヘッダーセルではその場に留まる |
| M-156 | 最終セルから先へTabすると行が追加される     | 最終行の最後のセルにカーソルを置き `Tab` を押す                                                                 | 同じ列数の空行が下に挿入され、その先頭セルにカーソルが移動する                                                                                     |
| M-157 | Enterで行追加、空行ではテーブルを抜ける     | 内容のあるテーブル行で `Enter` → できた空行でさらに `Enter`                                                     | 最初の `Enter` で下に空行が挿入されそこへ移動する。既に空のテーブル行で `Enter` を押すと空行（通常行）化してテーブルを抜ける                        |
| M-158 | テーブルブロックの整形                      | テーブル内にカーソルを置き `Ctrl/Cmd+Shift+L`（またはツールバーのテーブル整形ボタン）                           | テーブルブロック全体が整形される（列が揃い区切り行が正規化される）。内容は変わらない                                                               |
| M-159 | IME変換中はEnter/Tabを乗っ取らない *(要実機確認)* | テーブルのセルで日本語IME変換を開始し `Enter` で変換確定。変換中に `Tab` も押す                                 | EnterはIME変換の確定として働き（行追加しない）、変換中はTabも乗っ取られない。変換確定後はテーブル用のオーバーライドが復帰する                      |

## 27. ステータスバーのクイックトグルと単語数

| #     | テストケース             | 手順                                                                         | 期待結果                                                                                                                                                               |
| ----- | ------------------------ | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-160 | 単語数の表示             | 日本語と英語が混在するテキストを入力 → 一部を選択                             | 文字数の隣に単語数が表示される（CJKは1文字=1語、ラテン文字は空白区切りで数える）。選択範囲があるときは選択部分の単語数を表示する                                       |
| M-161 | ワードラップのクイックトグル | ステータスバー右側のワードラップアイコンをクリック                           | エディタのワードラップが即座に切り替わる。アイコンがON/OFF状態を反映し、設定 > エディタ > ワードラップ と同期し、再起動後も保持される                                  |
| M-162 | オートセーブのクイックトグル | ステータスバーのオートセーブ（保存）アイコンをクリック                       | オートセーブが即座に切り替わる。アイコンがON/OFF状態を反映し、設定 > 詳細 > オートセーブ と同期し、再起動後も保持される                                                |
| M-192 | 日本語の句読点が単語として数えられない *(リグレッション・実機確認)* | `これはペンです。` と `「引用」` をそれぞれ入力し、ステータスバーの単語数を見る | それぞれ 7 と 2 になる。CJKの句読点・記号（、。「」・！等）は区切りとして扱われ、単語として数えられない（日本語文書の単語数が水増しされない） |

## 28. Markdownプレビューのセキュリティ（HTML/CSSインジェクション）

| #     | テストケース                                                     | 手順                                                                                                                                                                                  | 期待結果                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| M-163 | CSSインジェクションのオーバーレイがアプリを乗っ取れない *(リグレッション — GHSA-5qr5-6vh4-6g2j)* | `<div style="position:fixed;top:0;left:0;width:100%;height:100%;z-index:99999">` の全画面オーバーレイ（および `<style>` ブロック）を含むMarkdown（CSSフィッシングPoC等）を開き、プレビュー／分割表示で確認 | 注入した要素が画面全体を覆わない：タブ・ツールバー・エディタ等のアプリUIは操作可能なまま、ファイルの編集・クローズもできる。`position:fixed/absolute` はサニタイズ時に除去され、残りもプレビュー枠内に封じ込められる。`<style>` ブロックは削除される |
| M-164 | エクスポートHTMLもオーバーレイ安全 *(リグレッション — GHSA-5qr5-6vh4-6g2j)* | 同じCSSインジェクションのMarkdownを開いた状態でHTMLエクスポートを実行し、生成された `.html` をブラウザで開く                                                                            | エクスポートしたファイルでも全画面オーバーレイ／偽ダイアログが表示されない。サニタイズはエクスポート経路でも実行されるため、オーバーレイ用のpositionや `<style>` ブロックが出力に含まれない                                       |

## 29. 画像の貼り付け＆ドラッグ&ドロップ挿入

ビットマップの貼り付けや画像ファイルのドラッグで画像を挿入する。書き出し／コピーした画像はドキュメント隣の `images/` サブフォルダに入る。ドキュメント配下に既にある画像はその場を相対参照する。相対パスの基準としてフォルダが要るため保存済みドキュメントが前提。Tauri のドラッグ&ドロップとファイル書き込みを使うため、実機（dev/本番ビルド）で確認する。

| #     | テストケース                                          | 手順                                                                                                                | 期待結果                                                                                                                                       |
| ----- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| M-179 | スクリーンショットを貼り付け *(実機確認)*             | 保存済みドキュメントで、スクリーンショットをクリップボードにコピーし、カーソルを置いて `Ctrl/Cmd+V`                  | ビットマップがドキュメント隣の `images/image-YYYYMMDD-HHMMSS.png` に書き出され、カーソル位置に `![](images/image-….png)` が挿入される。プレビューに画像が表示される |
| M-180 | ドキュメント配下の画像をドラッグ *(実機確認)*        | ドキュメントのフォルダ配下にある画像ファイルをエディタへドラッグ                                                    | その場を指す相対リンク（例 `images/foo.png` や `sub/foo.png`）がドロップ位置に挿入される。ファイルはコピーされない                             |
| M-181 | ドキュメント配下外の画像をドラッグ *(実機確認)*      | 無関係なフォルダの画像をエディタへドラッグ                                                                          | 画像が元の名前のまま `images/` へコピーされ、相対リンクが挿入される。同じファイルを再度落とすと再利用され、同名で内容が異なる画像は `-1` 連番が付く |
| M-182 | 複数画像を一度にドロップ *(実機確認)*                 | 複数の画像ファイルを選択してエディタへドロップ                                                                      | 各画像がドロップ位置に1行ずつ挿入される。スナックバーが挿入枚数を表示する                                                                     |
| M-183 | 未保存タブへ貼り付け/ドロップ *(実機確認)*           | 新規（未保存）タブで画像を貼り付け、またはドロップ                                                                  | 何も挿入されず、「先にドキュメントを保存してください」というスナックバーが出る（相対パスにはフォルダが必要）                                   |
| M-184 | ドキュメントのドロップは従来通り開く *(リグレッション・実機確認)* | `.md`/`.txt` をウィンドウにドラッグ。別途、画像でない非対応ファイルをドラッグ                                | Markdown/テキストはドキュメントとして開く（画像対応で開く挙動が壊れていない）。非対応の非画像ファイルは従来通り「Markdownファイルをドロップ」エラーになる |
| M-190 | スペース/非ASCIIの画像ファイル名が表示される *(リグレッション — B-10・実機確認)* | スペースや日本語を含む名前の画像（例 macOSのスクショ `スクリーンショット ….png`）を保存済みドキュメントへドラッグし、プレビューを見る | プレビュー（およびMarpプレビュー）で画像が表示される。相対 `<img src>` は marked がパーセントエンコードするが、ファイル読込前にデコードされるため、`test.png` と同様にスペース/非ASCII名も解決される |
| M-191 | 画像ドラッグ中のドロップ先ハイライト *(実機確認)* | 画像ファイルをエディタ領域の上までドラッグする（離す前の状態） | ドラッグ中はエディタ領域に破線の枠と「画像をここにドロップ」のヒントが表示され、ドロップ時または領域外へ出ると消える。`.md`/`.txt` のドラッグではこの画像ヒントは出ない |

## 30. エディタ内のスマートリスト編集

Monaco エディタ内でのMarkdownリストに対する Enter/Tab の処理。カーソルはリスト項目（`-`/`*`/`+`、`1.`/`1)`、または `- [ ]` タスク項目）上にあること。IME変換中とテーブル行上（セクション26が優先）では上書きが抑制される。

| #     | テストケース                                  | 手順                                                                                       | 期待結果                                                                                                             |
| ----- | --------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| M-185 | Enter で箇条書き／番号付きリストを継続        | `- 項目` の行で `Enter`、`3. 項目` の行で `Enter`、字下げした `    - 項目` で `Enter`        | 継続マーカー（`- `、番号は `4. ` にインクリメント）で新しいリスト行が作られ、元の字下げが保たれる                    |
| M-186 | 空項目での Enter でリストを終了               | リストを続けて空項目（`- ` / `1. ` / `- [ ] `）を作り、その行で `Enter`                     | 空のマーカーが空行にクリアされ、リストが終了する（それ以上マーカーは追加されない）                                  |
| M-187 | タスクリストの継続                            | `- [x] done` の行で `Enter`                                                                 | 未チェックの新しい `- [ ] ` 行が作られる                                                                            |
| M-188 | Tab / Shift+Tab でリスト項目を字下げ          | リスト項目上で `Tab`、続けて `Shift+Tab`                                                    | `Tab` で1タブ分字下げ、`Shift+Tab` で1タブ分戻る。桁0では `Shift+Tab` は何もしない                                  |
| M-189 | IME変換中は Enter を奪わない *(実機確認)*     | リスト行で日本語IMEの変換を開始し、確定のため `Enter`                                        | `Enter` はIME変換を確定し、新しいリスト項目を作らない。変換終了後はスマートリストの継続が再開する                    |
| M-193 | リスト行の先頭で Enter しても行が押し下がるだけでマーカーが重複しない *(リグレッション・実機確認)* | `- abc` の行の桁1（および `- ` マーカーの途中）にカーソルを置いて `Enter` | 行がそのまま下に押し下がり、上に空行ができる。`- - abc` のようなマーカー重複にならない。リスト継続はカーソルが項目コンテンツ以降にあるときだけ発動する |

## 31. 1.0.0 記念メッセージ（御礼ダイアログ）

1.0.0 マイルストーンの初回起動時に一度だけ表示される御礼ダイアログ。What's New（変更点リスト）とは別物で、意味を分けている。`seenMilestones` に `1.0.0` が記録されるまで表示され、閉じると What's New が続けて開く。表示記録は永続ストア（`seenMilestones` / `lastSeenVersion`）に保存されるため、再表示の検証時はこれらをクリアすること。

| #     | テストケース                                        | 手順                                                                                                     | 期待結果                                                                                                                                          |
| ----- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| M-196 | 記念メッセージ→What's New の順で表示 *(実機確認)*    | 表示記録をクリアした状態（`seenMilestones` 未設定・`lastSeenVersion` が `1.0.0` 以外）で 1.0.0 を起動      | まず 🎉 の御礼ダイアログ（「Bokuchi 1.0.0 をリリースしました」＋御礼文）が中央に表示される。「ありがとう」で閉じると、続けて What's New が開く（同時には出ない） |
| M-197 | 記念メッセージは一度きり *(実機確認)*               | M-196 の後にアプリを完全終了して再起動                                                                    | 御礼ダイアログは二度と表示されない（What's New も既読なら出ない）。ヘルプ等からの再表示導線もない                                                  |
| M-198 | 記念メッセージの多言語表示 *(実機確認)*             | 言語を切り替え（例: 英語・簡体字）、表示記録をクリアして 1.0.0 を起動                                      | 御礼ダイアログのタイトル・本文・「ありがとう」ボタンが選択言語で表示される（英語なら "Thank you"）。"Bokuchi" と "1.0.0" はそのまま                |
