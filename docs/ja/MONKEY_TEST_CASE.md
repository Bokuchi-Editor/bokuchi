# モンキーテストケースリスト

自動テストではカバーしきれないUI統合動作を手動で確認するためのテストケース。
本来はE2Eで実現したいが、今はTauriの対応状況的に不可能なので手動で行う。

## 1. タブ操作

| #    | テストケース                    | 操作手順                                       | 期待結果                                                |
| ---- | ------------------------------- | ---------------------------------------------- | ------------------------------------------------------- |
| M-01 | 新規タブ作成                    | `Ctrl+N` を押す                                | 新しいタブが開き、エディタにフォーカスが当たる          |
| M-02 | 複数タブ切り替え                | 3つ以上タブを開き、各タブをクリック            | 正しい内容が表示され、エディタにフォーカスが移る        |
| M-03 | Ctrl+Tab タブ切り替え           | `Ctrl+Tab` で次タブ、`Ctrl+Shift+Tab` で前タブ | 順序通りに切り替わり、末尾→先頭のラップも動作する       |
| M-04 | タブの閉じ（未変更）            | 未変更タブの×ボタンをクリック                  | 確認なしで即座に閉じる                                  |
| M-05 | タブの閉じ（変更あり）          | 内容を編集後、×ボタンをクリック                | 「保存しますか？」ダイアログが表示される                |
| M-06 | 保存ダイアログ - 保存           | M-05のダイアログで「保存」を選択               | ファイルが保存されてタブが閉じる                        |
| M-07 | 保存ダイアログ - 保存しない     | M-05のダイアログで「保存しない」を選択         | 保存せずタブが閉じる                                    |
| M-08 | 保存ダイアログ - キャンセル     | M-05のダイアログで「キャンセル」を選択         | ダイアログが閉じ、タブは残る                            |
| M-09 | 同一ファイルの重複オープン防止  | 同じファイルを2回開く                          | 2つ目は既存タブにフォーカスが移る（新タブは作られない） |
| M-10 | タブのドラッグ&ドロップ並べ替え | タブをドラッグして順序を変更                   | タブの並び順が変わり、永続化される                      |

## 2. ファイル操作

| #    | テストケース                     | 操作手順                                       | 期待結果                                                 |
| ---- | -------------------------------- | ---------------------------------------------- | -------------------------------------------------------- |
| M-11 | ファイルを開く                   | `Ctrl+O` → ファイル選択                        | ファイル内容がタブに表示され、成功通知が出る             |
| M-12 | ファイル保存                     | `Ctrl+S`                                       | 現在のタブが保存され、タイトルの変更マーク(●)が消える    |
| M-13 | 名前を付けて保存                 | `Ctrl+Shift+S`                                 | ダイアログで保存先を選択後、新しいファイル名でタブが更新 |
| M-14 | マークダウンファイルのドロップ   | `.md`ファイルをエディタ領域にドラッグ&ドロップ | ファイルが新しいタブで開かれる                           |
| M-15 | 非マークダウンファイルのドロップ | `.txt`ファイルをドロップ                       | 適切に処理される（エラーにならない）                     |

## 3. オートセーブ

| #    | テストケース           | 操作手順                                            | 期待結果                                   |
| ---- | ---------------------- | --------------------------------------------------- | ------------------------------------------ |
| M-16 | オートセーブ発動       | 設定でオートセーブON → 既存ファイルを編集 → 3秒待つ | 自動保存され、成功通知が出る               |
| M-17 | オートセーブ無効       | 設定でオートセーブOFF → 編集 → 3秒待つ              | 自動保存されない                           |
| M-18 | 新規タブでオートセーブ | 新規タブ（未保存ファイル）で編集 → 3秒待つ          | オートセーブは発動しない（パスがないため） |

## 4. 設定

| #    | テストケース       | 操作手順                       | 期待結果                       |
| ---- | ------------------ | ------------------------------ | ------------------------------ |
| M-19 | 設定画面を開く     | `Ctrl+,` を押す                | 設定ダイアログが開く           |
| M-20 | テーマ変更         | 設定でテーマを変更             | 即座にUIに反映される           |
| M-21 | 言語変更           | 設定で言語を変更               | UI全体のテキストが切り替わる   |
| M-22 | タブレイアウト変更 | 水平↔垂直を切り替え            | タブバーの表示方向が変わる     |
| M-23 | 設定の永続化       | テーマを変更 → アプリ再起動    | 変更したテーマが保持されている |
| M-24 | フォントサイズ変更 | エディタのフォントサイズを変更 | エディタのフォントが即座に反映 |
| M-25 | Mermaidレンダリング有効化 | 設定 > 詳細 > レンダリング拡張でMermaidをONに切り替え | 設定が保存され、mermaidフェンスドコードブロックがプレビューで図表として描画される |
| M-26 | KaTeXレンダリング無効化   | 設定 > 詳細 > レンダリング拡張でKaTeXをOFFに切り替え  | 数式（$...$, $$...$$）がプレビューでraw テキストとして表示される                  |
| M-27 | レンダリング設定の永続化  | レンダリングトグルを変更 → アプリ再起動               | 変更したレンダリング設定が保持されている                                          |

## 5. 表示モード

| #    | テストケース       | 操作手順                          | 期待結果                                          |
| ---- | ------------------ | --------------------------------- | ------------------------------------------------- |
| M-28 | 表示モード切り替え | `Ctrl+Shift+V` を繰り返し押す     | split → preview → editor → split とローテーション |
| M-29 | 同期スクロール     | splitモードでエディタをスクロール | プレビューが連動してスクロールする                |

## 6. キーボードショートカット

| #    | テストケース         | 操作手順              | 期待結果                       |
| ---- | -------------------- | --------------------- | ------------------------------ |
| M-30 | ヘルプ表示           | `F1` を押す           | ヘルプダイアログが開く         |
| M-31 | 最近のファイル       | `Ctrl+R` を押す       | 最近のファイルダイアログが開く |
| M-32 | アウトラインパネル   | `Ctrl+Shift+O` を押す | アウトラインパネルが開閉する   |
| M-33 | フォルダツリーパネル | `Ctrl+Shift+E` を押す | フォルダツリーパネルが開閉する |

## 7. アウトライン & フォルダツリー

| #    | テストケース                     | 操作手順                             | 期待結果                             |
| ---- | -------------------------------- | ------------------------------------ | ------------------------------------ |
| M-34 | アウトライン見出しジャンプ       | アウトラインパネルで見出しをクリック | エディタが該当行にスクロール         |
| M-35 | アウトライン自動更新             | エディタで見出しを追加/削除          | アウトラインパネルがリアルタイム更新 |
| M-36 | フォルダツリーからファイルを開く | フォルダツリーでファイルをクリック   | タブにファイルが開かれる             |
| M-37 | フォルダツリー展開/折りたたみ    | フォルダのアイコンをクリック         | サブツリーが展開/折りたたまれる      |

## 8. 外部ファイル変更検知

| #    | テストケース          | 操作手順                             | 期待結果                       |
| ---- | --------------------- | ------------------------------------ | ------------------------------ |
| M-38 | 外部変更検知          | 開いているファイルを別エディタで変更 | 変更検知ダイアログが表示される |
| M-39 | 外部変更 - リロード   | M-38のダイアログで「リロード」       | 最新の内容に更新される         |
| M-40 | 外部変更 - キャンセル | M-38のダイアログで「キャンセル」     | 現在の編集内容が保持される     |

## 9. ズーム

| #    | テストケース   | 操作手順        | 期待結果           |
| ---- | -------------- | --------------- | ------------------ |
| M-41 | ズームイン     | `Ctrl+=` を押す | UI全体が拡大される |
| M-42 | ズームアウト   | `Ctrl+-` を押す | UI全体が縮小される |
| M-43 | ズームリセット | `Ctrl+0` を押す | 100%に戻る         |

## 10. プレビューレンダリング

| #    | テストケース                       | 操作手順                                                                            | 期待結果                                                                  |
| ---- | ---------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| M-44 | Mermaid図表レンダリング            | Mermaidを有効にし、` ```mermaid ` フェンスドブロック（例: `graph TD; A-->B`）を記述  | プレビューにSVGとして図表が描画される                                     |
| M-45 | Mermaidエラーハンドリング          | 無効なmermaidブロック（例: `invalid!!!`）を記述                                      | 図表の代わりに赤いエラーメッセージが表示される                            |
| M-46 | Mermaidダークモードテーマ          | Mermaid図表を表示中にダークモードを切り替え                                         | 図表がダークテーマで再描画される                                          |
| M-47 | KaTeX数式レンダリング              | KaTeX有効の状態で `$E=mc^2$` と `$$\sum_{i=1}^{n} i$$` を記述                      | インライン・ディスプレイ数式が正しくレンダリングされる                    |
| M-48 | 無効化時のraw表示                  | KaTeXとMermaidを両方無効にし、数式とmermaidブロックを記述                            | `$...$` テキストと ` ```mermaid ` コードブロックがそのまま表示される      |
| M-49 | Mermaid付きHTMLエクスポート        | Mermaidを有効にし図表を記述、HTMLエクスポート                                       | エクスポートされたHTMLにMermaid図表がインラインSVGとして含まれる          |

## 11. エッジケース・安定性

| #    | テストケース     | 操作手順                          | 期待結果                            |
| ---- | ---------------- | --------------------------------- | ----------------------------------- |
| M-50 | 高速タブ切り替え | タブを素早く連打で切り替える      | クラッシュせず正しく切り替わる      |
| M-51 | 大量タブ         | 20個以上タブを開く                | スクロール/オーバーフロー処理が正常 |
| M-52 | 大きなファイル   | 1MB以上のMarkdownファイルを開く   | エディタが正常に動作する            |
| M-53 | 空ファイル       | 0バイトのファイルを開く           | エラーなく空のエディタが表示される  |
| M-54 | 長い行           | 1行が数千文字あるMarkdownファイル | ワードラップが正しく機能する        |
